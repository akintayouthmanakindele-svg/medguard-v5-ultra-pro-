# Copy this to .env.local for local dev — NEVER commit .env to GitHub

# Mono (Nigeria payments)
MONO_SECRET_KEY=live_sk_REPLACE_WITH_YOUR_MONO_SECRET_KEY
ALLOWED_ORIGIN=https://your-site.netlify.app

# Wise (International payments)
WISE_WEBHOOK_SECRET=REPLACE_WITH_WISE_WEBHOOK_SECRET
WISE_API_TOKEN=REPLACE_WITH_WISE_API_TOKEN

# Helio / MoonPay Commerce (Stablecoin)
HELIO_WEBHOOK_SECRET=REPLACE_WITH_HELIO_WEBHOOK_SECRET

# WalletConnect / TrustWallet
WC_PROJECT_ID=REPLACE_WITH_WALLETCONNECT_PROJECT_ID
SOLANA_RPC_URL=https://api.mainnet-beta.solana.com

# Database (Supabase — set up when ready for production)
SUPABASE_URL=https://YOUR_PROJECT.supabase.co
SUPABASE_SERVICE_KEY=REPLACE_WITH_SUPABASE_SERVICE_ROLE_KEY
